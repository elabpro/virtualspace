#JSGF V1.0;

grammar dialog;

<A> = (блокнот | браузер | новости| офис | почту);

<B> = (<A> | распознавание голоса | управление ладонью );


<s> = ((открой | запусти) <B> | отключи <B> | переключи приложение | закрой приложение);

<begin> = (проводник <s>);

public <command> = (<begin>);
